syntax = "proto3";

package packet;

enum Type
{
  NONE = 0;
  LOGIN_REQ = 1;
  CONNECT = 2;
  DISCONNECT = 3;
}

message Connect
{
  int64 network_id = 1;
}

message Disconnect
{
  int64 network_id = 1;
}

message Login
{
  int64 network_id = 1;
  string name = 2;
}